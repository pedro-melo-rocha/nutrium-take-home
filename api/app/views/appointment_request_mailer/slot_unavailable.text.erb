Hi <%= @request.guest_name %>,

The slot you requested with <%= @nutritionist.name %> is no longer available —
another appointment was confirmed for an overlapping time, so your request
couldn't be confirmed.

Requested service:  <%= @service.name %>
Requested time:     <%= @starts_at.strftime("%A, %B %d, %Y at %H:%M") %>

This was an automatic slot conflict, not a personal decision by
<%= @nutritionist.name %> — feel free to request another time.

— Nutri App
